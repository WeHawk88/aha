#CLEANING
# remove the packages that failed to install completely,
sudo apt-get autoclean

#to remove the apt-cache,
sudo apt-get clean

#remove the unwanted software dependencies,
sudo apt-get autoremove

